use this folder to write util functions that will be used in the main file!!!!


after cloneing the repo do ctrl + shift + p > python: create envirment > venv > python 3.13
then do pip install -r requirments.txt
